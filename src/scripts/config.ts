import { ShipConfig } from "./models";

export const PlayerShipConfig: ShipConfig = {
  position: { x: 10, y: 10 },
  size: { w: 50, h: 50 },
  background: 'url("/web/images/player.png")',
  className: 'player'
}
